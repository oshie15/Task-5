[build]
  publish = "client/build"
  command = "npm install && cd client && npm install && cd ../../client && npm run build"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200

[build.environment]
  NODE_VERSION = "18" 